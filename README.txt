###To Run, first install gem:
	(from the folder of the project) gem install imagesize

###Then use the following command:
	ruby resize.rb folder width height

IMPORTANT: 
	there can't be spaces into the file-names or the path
